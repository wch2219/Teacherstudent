<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- This is the main color -->
    <item

        >
        <shape>
            <!--    边框颜色 -->

            <solid android:color="@color/purple4"/>
            <solid android:color="@color/purple4" />
            <stroke
                android:width="1dp"
                android:color="@color/purple4"/>

        </shape>
    </item>

</layer-list>